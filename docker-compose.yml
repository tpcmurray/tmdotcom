services:
  db:
    image: postgres:16-alpine
    container_name: terrymurray-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: tmdotcom
      POSTGRES_PASSWORD: terrymurraydotcom_2026!
      POSTGRES_DB: terrymurray
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
